# Optional: override API base URL for frontend requests.
# Leave empty to use same-origin requests (default).
VITE_API_BASE_URL="http://127.0.0.1:8765"

# Optional: set to true to disable Vite HMR in unstable watch environments.
DISABLE_HMR="false"
